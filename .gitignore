# Cloudflare Pages / Wrangler
.wrangler
.dev.vars

# OS files
.DS_Store
Thumbs.db

# Large temporary assets / drafts we don't need in production repo
design_fetched.tar
untitled/
*.png
!logo.png
!favicon-*.png
!apple-touch-icon.png
!img/banks/**
!img/cases/**
*.pdf

# Allow assets inside slides directory
!slides/**/*.png
!slides/**/*.pdf
!slides/**/*.pptx
!slides/**/*.mov

# Python virtual environment (if any)
venv/
.venv/
__pycache__/

# Dependencies and build outputs
node_modules/
dist/
.env
.env.*


# PII - never deploy (contains resident registration & account numbers)
.agents/AGENTS.md

# Custom Agent Skills
# Local Secret Vaults
# 실수로 배포 디렉터리에 두면 https://ai-ing.org/<파일명> 으로 그대로 공개된다.
# 시크릿은 반드시 ~/.ai-ing-private/ 또는 1Password 등 레포 밖에 보관할 것.
AI_ING_SECURE_VAULT.md
*_vault.md
*secret*.md
*SECURE*.md

# 로컬 작업물 — 배포 대상이 아니다 (과거 로컬 wrangler 배포로 공개 노출된 이력 있음)
drafts/
models/
js/mingo-scroll.js

# Local backend service code
payment-backend/
.vercel
